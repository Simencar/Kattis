#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int N, T;
    cin >> N >> T;
    int money[N];
    int time[N];
    for (int i = 0; i < N; i++)
    {
        int m, t;
        cin >> m >> t;
        money[i] = m;
        time[i] = t;
        //hei
    }
}