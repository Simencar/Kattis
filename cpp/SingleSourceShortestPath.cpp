#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n, m, q, s;
    while((cin >> n >> m >> q >> s) && n != 0 && m != 0 && q != 0 && s != 0) {
        int unvisited[n];
    }



    return 0;
}